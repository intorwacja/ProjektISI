package org.example.projektisi.repositories;

import org.example.projektisi.entity.Liczba;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LiczbaRepository extends JpaRepository<Liczba, Long> {
}
